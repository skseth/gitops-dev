apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmGlobals:
  chartHome: ../.. # Points to the current directory
helmCharts:
- name: helm-chart
  # version: 1.0.0 # Version is not needed for local charts
  releaseName: aank
  namespace: apps
  valuesFile: values.yaml # Optional: specify a local values file
