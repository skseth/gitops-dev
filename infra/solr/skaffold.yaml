apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: infra-solr
manifests:
  kustomize:
    paths:
      - overlays/base
deploy:
  kubectl:
    defaultNamespace: infra

profiles:
  - name: dev
    activation:
      - kubeContext: rancher-desktop
    manifests:
      kustomize:
        paths:
          - overlays/dev
    portForward:
    - resourceType: service
      resourceName: solr-standalone-service
      port: 8983
      localPort: 8983

